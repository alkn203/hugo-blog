<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Onpointmove on Keep Coding</title>
    <link>https://alkn203.github.io/blog/tags/onpointmove/</link>
    <description>Recent content in Onpointmove on Keep Coding</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>ja</language>
    <lastBuildDate>Wed, 24 May 2017 23:19:06 +0900</lastBuildDate>
    <atom:link href="https://alkn203.github.io/blog/tags/onpointmove/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>タッチしている位置に徐々にSpriteを移動させる</title>
      <link>https://alkn203.github.io/blog/2017/05/24/touching-and-move</link>
      <pubDate>Wed, 24 May 2017 23:19:06 +0900</pubDate>
      
      <guid>https://alkn203.github.io/blog/2017/05/24/touching-and-move</guid>
      <description>

&lt;h2 id=&#34;今回のtips&#34;&gt;今回のTips&lt;/h2&gt;

&lt;p&gt;タッチしている位置に徐々にスプライトを移動させる方法について説明します。&lt;/p&gt;

&lt;p&gt;&lt;img src=&#34;https://alkn203.github.io/blog/images/onpointmove.gif&#34; alt=&#34;onpointmove.gif&#34; /&gt;&lt;/p&gt;

&lt;p&gt;&lt;center&gt;&lt;a href=&#34;http://runstant.com/alkn203/projects/de52206d&#34; target=&#34;_blank&#34;&gt;[runstantで確認]&lt;/a&gt;&lt;/center&gt;&lt;/p&gt;

&lt;h3 id=&#34;移動先を管理して一定の割合で減速させる&#34;&gt;移動先を管理して一定の割合で減速させる&lt;/h3&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;    // 移動先を管理する変数
    var targetX = sprite.x;
    var targetY = sprite.y;
    // タッチ開始
    this.onpointstart = function(e) {
      // 移動先を設定
      targetX = e.pointer.x;
      targetY = e.pointer.y;
    };
    // タッチ保持イベント
    this.onpointmove = function(e) {
      // 移動先を設定
      targetX = e.pointer.x;
      targetY = e.pointer.y;
    };
    // 更新
    this.update = function(app) {
      // タッチ中のみスプライトを動かす
      if (app.pointer.getPointing()) {
        // 徐々にタッチした位置に近づける
        var moveX = (targetX - sprite.x) * 0.25;
        var moveY = (targetY - sprite.y) * 0.25;
        sprite.moveBy(moveX, moveY);
      }
    };
&lt;/code&gt;&lt;/pre&gt;

&lt;ul&gt;
&lt;li&gt;移動先を管理する変数&lt;strong&gt;targetX&lt;/strong&gt;と&lt;strong&gt;targetY&lt;/strong&gt;を作成します。&lt;/li&gt;
&lt;li&gt;タッチ時に移動先を更新します。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;app.pointer.getPointing()&lt;/strong&gt;で現在タッチされてるかを取得できます。&lt;/li&gt;
&lt;li&gt;移動先とスプライトの現在の位置から方向ベクトルを求めて、一定割合で速度を減らして移動させます。&lt;/li&gt;
&lt;/ul&gt;

&lt;h3 id=&#34;コード&#34;&gt;コード&lt;/h3&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;// グローバルに展開
phina.globalize();
// アセット
var ASSETS = {
  // 画像
  image: {
    &#39;tomapiko&#39;: &#39;https://rawgit.com/phi-jp/phina.js/develop/assets/images/tomapiko.png&#39;,
  },
};
// 定数
var SPEED = 4;
/*
 * メインシーン
 */
phina.define(&amp;quot;MainScene&amp;quot;, {
  // 継承
  superClass: &#39;DisplayScene&#39;,
  // コンストラクタ
  init: function() {
    // 親クラス初期化
    this.superInit();
    // 背景
    this.backgroundColor = &#39;skyblue&#39;;
    // スプライト画像作成
    var sprite = Sprite(&#39;tomapiko&#39;).addChildTo(this);
    // 初期位置
    sprite.x = this.gridX.center();
    sprite.y = this.gridY.center();
    // 移動先を管理する変数
    var targetX = sprite.x;
    var targetY = sprite.y;
        // タッチ開始
    this.onpointstart = function(e) {
        // 移動先を設定
        targetX = e.pointer.x;
        targetY = e.pointer.y;
      };
    // タッチ保持イベント
    this.onpointmove = function(e) {
      // 移動先を設定
        targetX = e.pointer.x;
        targetY = e.pointer.y;
    };
    // 更新
    this.update = function(app) {
        // タッチ中のみスプライトを動かす
        if (app.pointer.getPointing()) {
            // 徐々にタッチした位置に近づける
            var moveX = (targetX - sprite.x) * 0.25;
            var moveY = (targetY - sprite.y) * 0.25;
            sprite.moveBy(moveX, moveY);
        }
      };
  },
});
/*
 * メイン処理
 */
phina.main(function() {
  // アプリケーションを生成
  var app = GameApp({
    title: &#39;徐々に移動&#39;,
    // MainScene から開始
    //startLabel: &#39;main&#39;,
    // アセット読み込み
    assets: ASSETS,
  });
  // fps表示
  //app.enableStats();
  // 実行
  app.run();
});
&lt;/code&gt;&lt;/pre&gt;
</description>
    </item>
    
  </channel>
</rss>